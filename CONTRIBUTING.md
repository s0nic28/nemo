# Contributing

1. Install dependencies with `npm install`.
2. Run `npm run dev`.
3. Keep UI behavior accessible and keyboard-usable.
4. Do not add secrets to the repository.
5. Keep camera/microphone features opt-in and transparent.
6. Use meaningful commit messages such as `feat: add cursor gaze smoothing`.
7. Add tests for logic that can be tested without a browser.
